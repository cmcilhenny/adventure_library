<div class="container">
<h2>Create A New Adventure</h2>

<%= form_for @adventure, :class => "form-horizontal" do |f| %>
  <div class="form-group">
	 <%= f.label :title, :class=>"col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.text_field :title, :autofocus => true, :placeholder => "title", :class=>"form-control" %><br>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :author, :class=>"col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.text_field :author, placeholder: "author", :class=>"form-control"  %><br>
    </div>
  </div>

  <%= f.fields_for :pages do |b| %>
  	<div class="form-group">
      <%= b.label "Page Name", :class=>"col-sm-2 control-label" %>
      <div class="col-sm-10">
  	   <%= b.text_field :name, placeholder: "start", :class => "form-control" %><br>
      </div>
    </div>
    <div class="form-group">
  	 <%= b.label :text, :class=>"col-sm-2 control-label" %>
  	 <div class="col-sm=10">
     <%= b.text_area :text, placeholder: "This is a great text adventure which is beginning right now!  I can't wait to [[see the end|end]]", :class => "form-control" %>
  	 </div>
    </div>
  <%end%>
  <br><%= f.submit :class => "btn btn-success pull-right" %>

<%end%>

<h2>How It Works</h2>
<p class="lead">Each page is linked to the next page via its 'page name.' When creating your adventure please include the following:</p>
<ul class="lead">
  <li>Fill in the Title field with the title of your adventure.</li>
  <li>Fill in the Author field with the name of the author of the adventure (most likely you!).</li>
  <li>Fill in the Page Name field in lowercase. It is important that the first page of your adventure is given the name "start".</li>
  <li>Fill in the Text field with text the adventure. Each page is linked using a special 'linkify' method. Be sure to create your link to the next page by enclosing it in brackets as follows: [[text that is a link|link]]. The first section of this method ('text that is a link') will be text that shows in your story. The second part ('link') is the name of the page the link will bring you to next. A sample is filled in above for guidance. </li>
  <li>Have fun (optional).</li>
</ul>
</div>
